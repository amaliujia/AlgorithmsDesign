/*
 * opt[n, k] =  min (max(opt[j, k-1], sum(j, n))).
 * Recursively compute the smallest maximum partition that assigned to patiner.
 * This smallest maximum partition is the upper bound time to solve this problem. 
 */
